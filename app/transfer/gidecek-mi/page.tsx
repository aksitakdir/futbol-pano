import TransferAbPage from "@/app/components/transfer/transfer-ab-page";

export const metadata = {
  title: "Gider mi? Transfer Anketi | Scout Gamer",
  description: "A mı B mi gidecek — transfer tahmin çubukları.",
};

export default function Page() {
  return <TransferAbPage locale="tr" />;
}
